<h1><%= @tag.name %></h1>

<% @articles.each do |article| %>
  <%= div_for article do %>
    <span class="date"><%= l article.published_at %></span>
    <%= link_to article.title, article %>
  <% end %>
<% end %>
